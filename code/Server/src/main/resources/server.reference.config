server {
  host = localhost
  record = false

  pool {
    size = 5
    display = Main
    // binary = /path/to/firefox/binary
    prefs {
      // int_pref = 4
      // bool_pref = true
      // str_pref = 'asdf'
    }
  }
}
